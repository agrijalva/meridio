app.controller("gesMateriaCtrl", ["$scope", "$sce", "$location","filterFilter","gesMateriaFactory", function($scope, $sce, $location, filterFilter, gesMateriaFactory ) {
    // $scope.DataUser     	= JSON.parse( localStorage.getItem("RCVUserData") );
    $scope.editorOptions = {
	    // settings more at http://docs.ckeditor.com/#!/guide/dev_configuration
	};

	
}]);