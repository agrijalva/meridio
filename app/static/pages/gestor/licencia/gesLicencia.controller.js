app.controller("gesLicenciaCtrl", ["$scope", "$sce", "$location","filterFilter","gesLicenciaFactory", function($scope, $sce, $location, filterFilter, gesLicenciaFactory ) {
    // $scope.DataUser     	= JSON.parse( localStorage.getItem("RCVUserData") );
    $scope.editorOptions = {
	    // settings more at http://docs.ckeditor.com/#!/guide/dev_configuration
	};

	
}]);