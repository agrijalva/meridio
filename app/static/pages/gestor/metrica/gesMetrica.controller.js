app.controller("gesMetricaCtrl", ["$scope", "$sce", "$location","filterFilter","gesTemaFactory", function($scope, $sce, $location, filterFilter, gesTemaFactory ) {
    // $scope.DataUser     	= JSON.parse( localStorage.getItem("RCVUserData") );
    $scope.editorOptions = {
	    // settings more at http://docs.ckeditor.com/#!/guide/dev_configuration
	};

	
}]);